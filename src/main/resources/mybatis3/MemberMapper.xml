<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
<!--    <insert id="insertMember" statementType="PREPARED" parameterType="sunmin.spring.vo.Member">-->
<!--        insert into member (userid, passwd, name, email) values (#{userid}, #{passwd}, #{name}, #{email})-->
<!--    </insert>-->

<!--    <select id="loginMember" statementType="PREPARED" parameterType="sunmin.spring.vo.Member" resultType="int">-->
<!--        select count(userid) cnt from member where userid = #{userid} and passwd = #{passwd}-->
<!--    </select>-->

<!--    <select id="selectOneMember" statementType="PREPARED" parameterType="String" resultType="sunmin.spring.vo.Member">-->
<!--        select * from member where userid = #{userid}-->
<!--    </select>-->
</mapper>
